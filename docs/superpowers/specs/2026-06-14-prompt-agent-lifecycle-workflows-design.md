# Archived: prompt lifecycle workflow design

This historical design note has been superseded by the current project contract in `AGENTS.md`.

Do not use this file as an active development source. Current behavior is:

- Formal outline generation is only the `outline_debate` product line.
- Prompt Catalog entries are structured prompt tasks bound to existing AgentSpecs or runtime seats.
- `35_chapter_prep_prompt.md` is a chapter-production support prompt and does not create a new Agent.
- Current tests enforce that legacy outline workflow IDs and historical Swarm outline surfaces are not exposed.
