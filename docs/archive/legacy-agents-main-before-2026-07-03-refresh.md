# Legacy AGENTS Snapshot Notice

This note preserves the provenance of the long MVP-era `AGENTS.md` contract that
was replaced on 2026-07-03 by the concise current project contract.

The full legacy text is available in Git history before this refresh, including
commit `50d80c4` at path `AGENTS.md`. Treat that older content as historical
context only. When it conflicts with the current root `AGENTS.md`, the current
root document wins.
