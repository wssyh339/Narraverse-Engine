# Agent Studio 1.0 前后端验收报告

验收日期：2026-06-05  
验收数据库：`backend/data/acceptance_20260605.db`  
结果：通过，存在环境限制与后续优化项。

## 自动化验证

| 项目 | 实际结果 | 状态 |
| --- | --- | --- |
| 后端完整 pytest | `14 passed, 1 warning` | 通过 |
| 前端契约测试 | `11 passed` | 通过 |
| TypeScript 检查 | `tsc --noEmit` 通过 | 通过 |
| 前端生产构建 | Vite 构建通过 | 通过 |
| 部署文件静态检查 | `.env.example`、README、Dockerfile、`docker-compose.yml`、scripts 完整 | 通过 |
| Docker Compose 容器实跑 | 当前机器未安装 Docker，仅完成 YAML 静态解析 | 环境限制 |
| 真实 LLM Provider 冒烟 | 未配置 API Key，未调用远程 Provider | 环境限制 |

## 真实端到端验证

| 操作 | 预期结果 | 实际结果 | 状态 |
| --- | --- | --- | --- |
| Dashboard 创建项目并进入工作台 | 创建成功并跳转工作台 | 成功进入 `/projects/{id}/workspace` | 通过 |
| 编辑角色、实体/物件、世界观 | 数据可提前维护 | 页面正确展示和更新设定 | 通过 |
| Agent 辅助生成候选设定 | 生成候选角色/设定 | 生成 3 条候选角色卡并展示成功提示 | 通过 |
| 生成章节规划 | 生成章节树 | 成功生成 3 章 | 通过 |
| 生成第一章正文 | 多 Agent 生成正文并保存轨迹 | 正文生成成功，记录 10 个 Agent 执行节点 | 通过 |
| AI 提案审批 | 提案不直接覆盖正文 | 先展示 diff，确认后应用并保存快照 | 通过 |
| 查看设定更新与图谱 | 图谱非空且可渲染 | ECharts 画布为 `1202x640`，节点数据可用 | 通过 |
| 伏笔创建、回收、删除 | 全流程可操作 | 创建、标记回收、删除后空状态均正常 | 通过 |
| 工作流图与提示词编辑 | 节点可点击、提示词可保存 | 控制节点说明和 Agent 提示词均保存成功 | 通过 |
| 版本对比与回滚 | 显示 diff 并可回滚 | diff 正确显示，回滚 API 与确认流程成功 | 通过 |
| 重复批量生成 | 多次执行不报错 | 修复幂等键冲突后重复生成成功 | 通过 |
| 批量暂停、恢复、取消 | 状态可控制 | `paused`、`queued`、`cancelled` 状态依次生效 | 通过 |
| Markdown 导出 | 生成 `.md` 文件与预览 | 成功生成并展示文件路径 | 通过 |
| TXT 导出 | 生成 `.txt` 文件与预览 | 成功生成并展示文件路径 | 通过 |
| 删除测试项目 | 确认后删除并刷新列表 | 项目总数和活跃项目刷新为 0 | 通过 |

## 边界与故障验证

| 场景 | 实际结果 | 状态 |
| --- | --- | --- |
| 空输入 | 创建向导阻止进入下一步，显示字段提示 | 通过 |
| 5000 字超长标题 | 后端稳定返回统一 `422 VALIDATION_ERROR` | 通过 |
| 重复章节规划 | 相同幂等键复用同一个 job，不产生重复章节 | 通过 |
| 重复批量生成 | 不再触发 `KeyError: chapter`，每次创建独立 draft job | 通过 |
| 后端不可用 | Dashboard 显示“无法连接后端服务，请确认后端已启动。” | 通过 |
| 无 API Key | 本地规则化工作流正常完成，README 明确说明回退行为 | 通过 |
| 空数据页面 | Dashboard、工作台、伏笔页均显示明确空状态 | 通过 |
| 加载/生成状态 | 创建、生成、批量、导出均有 loading 或进度状态 | 通过 |
| WebSocket | `/ws/progress` 与 `/ws/jobs/{job_id}` 连接消息测试通过 | 通过 |

## 本次修复

- 修正前端路由与 README 契约测试。
- 修正创建向导当前步骤校验和重复点击保护。
- 修正无项目时侧栏菜单重复 key。
- 补全统一工作台样式、AI 提案审批和 Agent 图节点可访问入口。
- 增加版本管理页回滚按钮、确认弹窗、成功和错误提示。
- 增加批量页真实 job 状态、暂停、恢复、取消操作。
- 修复 Ant Design 应用上下文缺失导致成功回调被误判失败。
- 修复重复批量生成复用章节 draft 幂等键导致的后端 500。
- 修复批量任务完成进度被错误重置为 `1/1`。

## 已知限制

- 批量生成当前仍为同步本地流程；暂停、恢复、取消会更新任务状态，但不能中断已同步完成的执行。
- 未配置 OpenAI、DeepSeek 或 Qwen API Key，本次未执行真实远程 Provider 冒烟测试。
- 当前机器没有 Docker，未执行 `docker compose up --build`。
- 前端生产包存在大于 500 kB 的 chunk 警告，建议后续按页面拆包。
- 测试存在 Starlette `httpx` 兼容性弃用警告，不影响当前运行。

## 验收访问地址

- 前端：`http://localhost:5173`
- 后端 API：`http://localhost:8000/api`
- OpenAPI 文档：`http://localhost:8000/docs`
