[project]
name = "narraverse-engine-backend"
version = "0.2.0"
description = "Backend service for Narraverse Engine, a local-first long-form novel inference studio."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
