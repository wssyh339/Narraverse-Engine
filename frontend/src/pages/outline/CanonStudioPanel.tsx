import type { Project, StoryBible } from "../../types/api";

interface CanonStudioPanelProps {
  projectId: string;
  project?: Project;
  storyBible?: StoryBible;
}

export function CanonStudioPanel(_props: CanonStudioPanelProps) {
  return null;
}
