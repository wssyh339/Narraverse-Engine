# 安全策略

叙界推演引擎 / Narraverse Engine 是本地优先项目，但仍然会处理敏感数据：

- 未发布手稿；
- API Key；
- 提示词和模型输出；
- 本地 SQLite 数据库；
- 导出文件和任务 artifacts。

## 支持版本

在项目达到稳定发布前，`main` 分支是主要维护线。安全修复优先进入 `main`。

## 报告安全问题

请不要在公开 issue 中披露密钥泄露、远程代码执行、路径穿越或数据泄露问题。

报告时请尽量提供：

- 受影响的版本或 commit；
- 复现步骤；
- 可能影响；
- 是否会暴露密钥、本地文件或手稿数据；
- 如果你有修复建议，也请一并说明。

## 安全边界

- API Key 必须只来自环境变量。
- 前端代码不得读取或存储 LLM API Key。
- `.env`、本地数据库、生成导出物和任务 artifacts 不应被提交。
- 导出路径和文件下载应限制在配置的 artifact 目录内。
- AI 生成变更必须保留来源，且不应在未确认时覆盖用户数据。
